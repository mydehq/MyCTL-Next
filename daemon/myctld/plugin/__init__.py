"""Daemon plugin runtime support.

Contains loader, discovery manager, and shared plugin runtime models used by
the daemon registry.

Modules in this package follow postponed annotation evaluation for consistent
type hint behavior.

Read before: daemon/myctld/plugin/models.py; read after: daemon/myctld/app.py
"""
