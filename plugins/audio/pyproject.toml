[project]
name = "audio"
version = "1.0.0"
description = "Manage PulseAudio/Pipewire sinks and sources"
dependencies = ["pulsectl-asyncio"]

[tool.myctl]
api_version = "2.0.0"
entry = "main.py"

[tool.myctl.groups]
"volume" = "Control sink volume levels"
